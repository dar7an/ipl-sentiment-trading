# Archive — not part of the runtime app

`data_collection/` is the 2024 scraping / trimming / chunking experiment.

It is **not** how `data/chunks` was produced (that script emits a different schema), several files do not import, and some paths point at an old checkout. Do not run these to refresh the paper book.

The frozen research record is `data/chunks`, `data/balls`, and `data/odds`. Leave those JSON files unchanged.
